# pipelines/azure-pipelines-release.yml
#
# Release Pipeline -- applies the EXACT plan artifact the Build Pipeline
# produced. Never regenerates a plan. Manually triggered, never automatic.
#
# Staging's actual Terraform work runs in a PLAIN job, not a deployment job --
# the Microsoft DevLabs Terraform extension has a known bug (confirmed via
# microsoft/azure-pipelines-extensions issue #724) where TerraformTaskV4 fails
# with "spawn ... ENOENT" specifically inside deployment job runOnce.deploy
# steps, even though the same binary/task works correctly in a plain job. A
# separate, lightweight deployment job records the deployment against the
# 'staging' Environment afterward, without touching Terraform.
#
# Production has no apply at all -- it's a deliberate human checkpoint that
# confirms staging succeeded, gated by an approval on the 'production'
# Environment.

trigger: none

resources:
  pipelines:
    - pipeline: buildPipeline
      source: 'satyadeep11singh.insurance-claims-platform-build'
      branch: main

pool:
  vmImage: 'ubuntu-latest'

variables:
  azureServiceConnection: 'claims-platform-oidc-connection'
  terraformWorkingDirectory: '$(System.DefaultWorkingDirectory)/environments/dev'

stages:
  - stage: Staging
    displayName: Staging - apply infrastructure
    jobs:
      - job: ApplyStaging
        displayName: terraform apply (staging)
        steps:
          - task: TerraformInstaller@1
            displayName: Install Terraform
            inputs:
              terraformVersion: '1.7.5'

          - download: buildPipeline
            artifact: terraform-plan
            displayName: Download plan artifact (from Build Pipeline)

          - task: TerraformTaskV4@4
            displayName: terraform init
            inputs:
              provider: 'azurerm'
              command: 'init'
              workingDirectory: '$(terraformWorkingDirectory)'
              backendServiceArm: '$(azureServiceConnection)'
              backendAzureRmResourceGroupName: 'rg-northwind-tfstate'
              backendAzureRmStorageAccountName: 'stnorthwindtf676746'
              backendAzureRmContainerName: 'tfstate'
              backendAzureRmKey: 'claims-platform-dev.tfstate'

          - task: TerraformTaskV4@4
            displayName: terraform apply (exact plan artifact)
            inputs:
              provider: 'azurerm'
              command: 'apply'
              workingDirectory: '$(terraformWorkingDirectory)'
              environmentServiceNameAzureRM: '$(azureServiceConnection)'
              commandOptions: '$(Pipeline.Workspace)/buildPipeline/terraform-plan/tfplan'

      - deployment: RecordStaging
        displayName: Record staging deployment
        dependsOn: ApplyStaging
        environment: 'staging'
        strategy:
          runOnce:
            deploy:
              steps:
                - script: echo "Staging apply completed by job ApplyStaging."
                  displayName: Record staging deployment

  - stage: Production
    displayName: Production - confirm deployment
    dependsOn: Staging
    condition: succeeded()
    jobs:
      - deployment: ConfirmProduction
        displayName: Confirm staging deployment for production
        environment: 'production'
        strategy:
          runOnce:
            deploy:
              steps:
                - script: |
                    echo "Staging apply succeeded and has been reviewed."
                    echo "Confirming claims platform deployment as production-approved."
                  displayName: Record production confirmation